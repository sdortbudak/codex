# Car Gallery App

This is a minimal web-based car dealership management system built with Flask and SQLite.

## Requirements

- Python 3.11+
- `pip install -r requirements.txt`

## Usage

Run the app using:

```bash
python app.py
```

The server will start at `http://localhost:5000`.
